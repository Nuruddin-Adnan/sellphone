import React from 'react';
import './Loader.css';

const Loader = () => {
    return (
        <div id="preloader-box">
            <div id="loader-spiner"></div>
        </div>
    );
};

export default Loader;